gcp-credentials:
  serviceaccount.json: (( read( "./gcloud.json", "text" ) ))

aws-credentials:
  accessKeyID: (( env( "ACCESS_KEY_ID" ) ))
  secretAccessKey: (( env( "SECRET_ACCESS_KEY_ID" ) ))

azure-credentials:
  clientID: (( env( "AZ_CLIENT_ID" ) ))
  clientSecret: (( env( "AZ_CLIENT_SECRET" ) ))
  subscriptionID: (( env( "AZ_SUBSCRIPTION_ID" ) ))
  tenantID: (( env( "AZ_TENANT_ID" ) ))

k8sVersions:
  kubernetes:
    versions:
    - 1.14.3


############################################################################


landscape:
  name: (( ( env( "GARDENER_PREFIX" ) || "test" ) "-gardener-oq" ))

  versions:
    gardener:
      apiserver:
        image_tag: (( env( "GARDENER_VERSION" ) || read( __ctx.DIR "/crop/GARDENER_VERSION", "text" ) ))
      controller_manager:
        image_tag: (( env( "GARDENER_VERSION" ) || read( __ctx.DIR "/crop/GARDENER_VERSION", "text" ) ))
      tag: (( env( "GARDENER_VERSION" ) || read( __ctx.DIR "/crop/GARDENER_VERSION", "text" ) ))

  domain: (( ( env( "GARDENER_PREFIX" ) || "test" ) ".oq.gcp.dev.k8s.ondemand.com" ))

  cluster:
    networks:
      nodes: 10.132.0.0/16
      pods: 10.28.0.0/15
      services: 10.31.240.0/20

  iaas:
  - name: gcp
    mode: seed
    type: gcp
    region: europe-west1
    zones:
      - (( region "-b"))
      - (( region "-c"))
      - (( region "-d"))
    credentials:
      <<: (( .gcp-credentials ))
    profile:
      gcp:
        constraints:
          <<: (( .k8sVersions ))
  - name: aws
    mode: cloudprofile
    type: aws
    region: eu-west-1
    zones:
      - (( region "a"))
      - (( region "b"))
      - (( region "c"))
    credentials:
      <<: (( .aws-credentials ))
    profile:
      aws:
        constraints:
          <<: (( .k8sVersions ))
  - name: azure
    mode: cloudprofile
    type: azure
    region: westeurope
    credentials:
      <<: (( .azure-credentials ))
    profile:
      azure:
        constraints:
          <<: (( .k8sVersions ))

  identity:
    users:
      - email: "admin@example.com"
        username: "example"
        password: (( rand("A-Z", 10) ))


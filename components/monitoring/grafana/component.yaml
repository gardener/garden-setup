---
landscape: (( &temporary ))
component:
  active: (( .landscape.monitoring.active ))
  imports:
    - prometheus: monitoring/prometheus
    - ingress-controller
    - cert: cert-management/cert
    - namespace

  stubs:
    - plugins/kubectl/utilities.yaml